package swe619.java;

import java.util.HashSet;
import java.util.Set;

public class Main {
    public static void main(String[] args) {




        {/*
                Set<String> r = new HashSet<>();
        r.add("ant");
        r.add("bee");
        // r: [ant, bee]

        Set<String> sh = new InstrumentedHashSet<>();
        sh.addAll(r);
        // sh: [ant, bee]

        Set<String> s =  new InstrumentedSet<>(r); //wrapper around r
        s.add("ant");
        s.add("cat");
        // s: [ant, bee, cat]
        // r: [ant, bee, cat]
        //addCount = 3

        Set<String> t = new InstrumentedSet<>(s); //wrapper around s and wrapper around r
        t.add("dog"); //s is back by r, t is back by s which is backed by r
        // -- any call to overridden add from t,s are ops on r because of ForwardingSet. r is 'real' set
        // s: [ant, bee, cat, dog]
        // r: [ant, bee, cat, dog]
        // t: [ant, bee, cat, dog]
        //adding only one to t so addCount = 1

        r.remove("bee");
        // s: [ant, cat, dog]
        // r: [ant, cat, dog]
        // t: [ant, cat, dog]

        s.remove("ant");
        // s: [cat, dog]
        // r: [cat, dog]
        // t: [cat, dog]

        //equals satisfying Set interface equals contract (says if they are both sets and have same things then they are equal)

//        MComplex m = MComplex.MComplexFactory(1,2);
//        System.out.println(m);
//        MComplex x = m.add(m);
//        System.out.println(x);
        */}

    }
}
